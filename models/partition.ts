/**
 * A code snippet split into its top-level import declarations (hoistable to
 * the surrounding module) and the rest of its statements.
 */
export type Partition = { imports: string[]; body: string }
